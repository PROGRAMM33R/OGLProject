#pragma once

#include "Std.hpp"

namespace config {

	const string      WINDOW_LABEL = "BachelorProject";
	const int         WINDOW_WIDTH = 1024;
	const int         WINDOW_HEIGHT = 568;
	const glm::vec3   INIT_CAMERA_POSITION = glm::vec3(0, 20, 30);
	const float       FOV = 45.0f;
	const float       SPEED = 30.0f;
	const float       MOUSE_SPEED = 0.005f;

}